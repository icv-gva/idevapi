# Tasks: idevapi-1.3.21

## Review Workload Forecast

| Item | Count / Estimate |
|---|---|
| Library source changes (resolver + IDEVAPIVersion + `package.json`) | ~67 lines diff |
| Minified artifacts rebuilt (`npm run build` → 18 `*-min.*`) | 18 files; ~1,800 lines `git diff --stat` (avg ~100 lines/file of minified churn) |
| Visor files in `_visores_tester/` (outside git) | 33 (29 on legacy `../../1.3/`, 4 already on `../../idevapi/`); ~7 lines diff each ≈ 231 lines |
| Visor files in `ayuda/ejemplos/1.3/` (outside git) | **0** — out of scope for 1.3.21, deferred to future help SDD |
| Migration script (`scripts/migrate-idevapi-1.3.21-visors.ps1`) | new file, ~50 lines |
| Junction deletion (`D:\antigravity\idevapi\1.3`) | filesystem only, 0 diff |
| **Total estimated diff (library + script, what goes through PR review)** | **~1,917 lines** |
| **400-line budget risk** | **High** (4.8× the budget) |

```text
Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: High
PR 2 size:exception: ACCEPTED by user
```

### PR Plan (chain strategy = `stacked-to-main`, PR 2 uses `size:exception`)

| PR | Scope | Approx. diff | Base | Notes |
|---|---|---|---|---|
| **PR 1** | `js/idevAPI_core.js` resolver rewrite (lines 18–50) + `IDEVAPIVersion` → `"1.3.21"` (line 75) + `package.json` bump | **~67 lines** | `main` | Under budget on its own. Merges first; may tag as `1.3.21-rc` for jsdelivr to start caching. |
| **PR 2** | 18 rebuilt `*-min.*` artifacts + new `scripts/migrate-idevapi-1.3.21-visors.ps1` | **~1,850 lines** | `main` (after PR 1 merged) | **`size:exception` accepted by user** — diff is mechanical, generated by `npm run build`; reviewers verify by build artifact (4 literals in `idevAPI_core-min.js`) not line-by-line. |
| (outside PR) | Visor migration (33 files in `_visores_tester/`, 0 in `ayuda/`) + junction delete + tag `1.3.21` + jsdelivr smoke test | filesystem + release action | n/a | Not in PR workflow. Committed to repo only as migration script (PR 2) and release tag. |

### Visor migration (outside PR)

Visors live in `D:\antigravity\idevapi\_visores_tester\` (parent of repo, 33 visors with IDEVAPI selector). These edits are **filesystem, never `git add`**. Treated as a separate work unit, not a PR. The PowerShell migration script is committed in PR 2 (auditable) but the visor outputs are not. The 27 examples in `ayuda/ejemplos/1.3` are deferred to a future help SDD (memory: `idevapi/help-system-future-sdd`).

## Phase 1: Library source (PR 1)

### Task 1.1: Bump version constants
- [x] `package.json` line 3: `"version": "1.3.20"` → `"1.3.21"`.
- [x] `js/idevAPI_core.js` line 75: `var IDEVAPIVersion = "1.3.20";` → `"1.3.21"`.
- **Verify**: `cat package.json` shows `1.3.21`; `grep IDEVAPIVersion js/idevAPI_core.js` shows `1.3.21`.
- **Reqs**: REQ-REL-1, REQ-REL-2.
- **Commit shape** (per `work-unit-commits`): pair with Task 1.2 in a single `feat(resolver): rewrite URL detection for 3-layer load contract` commit — the resolver uses `URLVersion` implicitly through the cache-buster, and `IDEVAPIVersion` only changes when the resolver release ships.

### Task 1.2: Rewrite URL resolver
- [x] `js/idevAPI_core.js` lines 18–50: replace the 6-branch GVA chain + parallel `URLVersion` switch + `/1.[0-3]/` regex with `detectLoadFamily(srcCore)` + `resolveJsdelivr` + `resolveGVA` + `resolveLocal` per design §Resolver structure. Line 53 (`urlAPI += URLVersion`) stays unchanged.
- **Verify**: `git diff js/idevAPI_core.js` is contained to lines 18–53 + line 75. No other source touched.
- **Reqs**: REQ-URL-1..9.
- **Commit shape**: single commit with Task 1.1 (Conventional Commit `feat(resolver): ...`).

## Phase 2: Build (PR 2)

### Task 2.1: Run `npm run build`
- [x] Working dir: `D:\antigravity\idevapi\idevapi`.
- **Verify**: process exits 0; each of the 18 `*-min.*` artifacts (per `build.js` entries list) has a timestamp newer than its source; `js/idevAPI_config-min.js` present (18th entry, kept per `build.js` comment).
- **Reqs**: REQ-REL-3 (exit-0 scenario).
- **Commit shape**: per `work-unit-commits`, this is one deliverable (a rebuild). Single commit `chore(build): rebuild 18 minified artifacts for 1.3.21`. Bundles all 18 minified files together — splitting them across commits is a "by file type" anti-pattern (skill rule).

### Task 2.2: Inspect minified core
- [x] `js/idevAPI_core-min.js`: contains the literal substrings `cdn.jsdelivr.net/gh`, `geoidevapi-dsa.gva.es`, `idevapi\/js\/idevAPI_core`, and `1.3.21`.
- **Reqs**: REQ-REL-3 (resolver + version embedding scenarios).
- **Verify**: `Select-String` over `js/idevAPI_core-min.js` matches all four literals.

## Phase 3: Visor migration (filesystem, outside PR)

### Task 3.1: Author PowerShell migration script
- [x] File: `D:\antigravity\idevapi\idevapi\scripts\migrate-idevapi-1.3.21-visors.ps1` (new, committed to repo for auditability).
- **Behaviour**: glob `_visores_tester/**/*.html` + `ayuda/**/*.html`; regex-match the selector block from `var currentDomain` through the `document.write('<script src="' + scriptSrc + '"><\/script>');` line; replace with the canonical 15-line block from design §Selector template; print report (files modified, skipped, outliers).
- **Verify**: dry-run on a single visor (e.g. `geoidev-mapas_electorales/02_partido_gobernante.html`) produces the canonical 5-branch block. Script committed.

### Task 3.2: Run migration on `_visores_tester/`
- [x] **Verify**: post-check `Select-String -Pattern '../../1.3/'` over `_visores_tester/**/*.html` returns 0 matches; every one of the 33 visors contains exactly one `scriptSrc = "../../idevapi/js/idevAPI_core.js"` line and the canonical 5-branch block. Script report shows 33 modified, 0 outliers.
- **Reqs**: REQ-VIS-1..7, REQ-VIS-9.

### Task 3.3: (DEFERRED) `ayuda/ejemplos/1.3` migration
- **Status**: **DEFERRED to future help-system SDD.** The 27 examples in `D:\antigravity\idevapi\ayuda\ejemplos\1.3\` use a different selector pattern (local / FTP / DSA / PRE / PRO, no jsdelivr, fixed script tag in help code display) and are out of scope for 1.3.21.
- **Apply phase action**: none. PowerShell migration script globs `_visores_tester/` only (NOT `ayuda/`).
- **Context for future SDD**: see memory `idevapi/help-system-future-sdd`.

## Phase 4: Junction cleanup

### Task 4.1: Delete Windows junction
- [x] `Remove-Item D:\antigravity\idevapi\1.3` (junction, not a real folder — confirmed via `Get-Item` ReparsePoint=true, LinkType=Junction, Target=`d:\antigravity\idevapi\idevapi`).
- **Verify**: `Test-Path D:\antigravity\idevapi\1.3` returns `False`; visors still load from `../../idevapi/`.
- **Reqs**: REQ-REL-5.

## Phase 5: Tag & push (release action, no PR)

### Task 5.1: Commit and tag 1.3.21
- [x] `git add js/idevAPI_core.js package.json js/*-min.js js/patches/*-min.js css/*-min.css wg/*-min.js wg/*-min.css lf_194/plugins/*-min.js`; commit the release; `git tag 1.3.21`; `git push origin main`; `git push origin 1.3.21`.
- **Verify**: `git tag --list 1.3.21` shows the tag; `git ls-remote --tags origin 1.3.21` returns the SHA; both pushes exit 0.
- **Reqs**: REQ-REL-4.

### Task 5.2: jsdelivr reachability smoke (within 24h, soft check)
- [ ] `Invoke-WebRequest https://cdn.jsdelivr.net/gh/icv-gva/idevapi@1.3.21/js/idevAPI_core-min.js` → status 200; response body contains `1.3.21`.
- **Reqs**: REQ-REL-6.

## Phase 6: Manual smoke test

### Task 6.1: Per-form jsdelivr verification
- [x] Test files created for all 5 forms (`@1.3.19`, `@1.3.20`, `@1.3.21`, `@1.3`, `@1`).
- [ ] Automated assertion pending: load each minimal HTML page (`<script src="…"></script>` + empty `<div id="map"></div>`) and assert: console clean (no IDEVAPI errors/warnings) **AND** `document.querySelector('.leaflet-container') !== null` after `DOMContentLoaded`. Manual verification required if no browser is available.
- **Reqs**: REQ-URL-1 (5 scenarios), REQ-URL-2.

### Task 6.2: Per-visor verification
- [x] Representative visor selected: `geoidev-mapas_electorales/02_partido_gobernante.html`.
- [ ] Automated assertion pending: open via `file://` and assert clean console + Leaflet container.
- **Reqs**: REQ-REL-9.
- **Note**: `ayuda/` is out of scope for 1.3.21; no per-help-example verification in this SDD.

## Phase 7: Conditional backport (user-gated)

### Task 7.1: Evaluate retro-compat
- [x] 1.3.19 — compatible (same `srcCore` discovery and `urlAPI += URLVersion` structure).
- [x] 1.3.20 — compatible (same structure as 1.3.19 plus legacy local regex fallback).

### Task 7.2: Backport resolver (per tag, gated)
- [ ] **Skipped pending explicit user confirmation per tag** (REQ-REL-7). Both tags are 1:1 compatible, but no known external consumers exist and force-pushing prior tags is destructive.

## Resolved (Q1-Q3 closed in interactive session)

| # | Question | Decision |
|---|----------|----------|
| Q1 | `ayuda/` migration | **DEFERRED** to future help-system SDD. 27 examples in `D:\antigravity\idevapi\ayuda\ejemplos\1.3\` use a different selector pattern (local / FTP / DSA / PRE / PRO, no jsdelivr). Memory: `idevapi/help-system-future-sdd`. |
| Q2 | Chain strategy | **`stacked-to-main`** — PR 1 merges first, PR 2 stacks on top |
| Q3 | PR 2 size:exception | **ACCEPTED** by user — minified diff is mechanical, reviewers verify by build artifact |

## Open Questions for sdd-apply

1. **Migration script location.** `scripts/migrate-idevapi-1.3.21-visors.ps1` is proposed; confirm path before commit. (Default: `scripts/` in the repo, audit-friendly.)
2. **Backport necessity for 1.3.19 / 1.3.20.** Deferred to apply phase (Task 7.1). If both tags have no known external consumers, recommend skipping the backport to avoid destructive force-push.
